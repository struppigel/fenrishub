[build]
buildCommand = "python manage.py collectstatic --noinput"

[deploy]
preDeployCommand = "python manage.py test --parallel && python manage.py migrate && python manage.py ensure_superuser"
startCommand = "gunicorn fenrishub.wsgi:application --bind 0.0.0.0:$PORT"
